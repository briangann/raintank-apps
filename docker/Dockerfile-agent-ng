# use ubuntu image
FROM rta-server:latest
#
ENV HOME /app

# Copy the built binaries into /app
RUN mkdir -p /app
COPY --from=rta-server /app/task-agent-ng /app

# Run task-server
WORKDIR /app
CMD ["./task-agent-ng", "-config", "/etc/raintank/task-agent.ini"]
